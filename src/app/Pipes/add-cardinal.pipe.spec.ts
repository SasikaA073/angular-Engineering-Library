import { AddCardinalPipe } from './add-cardinal.pipe';

describe('AddCardinalPipe', () => {
  it('create an instance', () => {
    const pipe = new AddCardinalPipe();
    expect(pipe).toBeTruthy();
  });
});
